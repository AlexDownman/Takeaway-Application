package DatabaseManager;

public enum ColumnDefinitions {
    INT,
    INTEGER,
    TINYINT,
    CHAR,
    CLOB,
    TEXT,
    BLOB,
    REAL,
    DOUBLE,
    FLOAT,
    NUMERIC,
    DECIMAL,

    NULL,
    NOT_NULL,
    PRIMARY_KEY,
    FOREIGN_KEY,
    UNIQUE,
    AUTO_INCREMENT
}
